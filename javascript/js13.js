 let randomValue= {
        name:'amith'
    }
    randomValue =23;
    if(!typeof randomValue==='string'){  //negation mark convert number into boolean(false)
        console.log('it is not a string')
    }else{
        console.log('it is string ')  //ans is it is a string
    }